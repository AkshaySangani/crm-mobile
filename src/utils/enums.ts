

// define enum for roles
export enum RoleEnum {
  EMPLOYEE = "EMPLOYEE",
  MANAGER = "MANAGER",
  ADMIN = "ADMIN",
  OWNER = "OWNER",
}

// define enum for all status types
export enum statusEnum {
  ACTIVE = "ACTIVE",
  INACTIVE = "INACTIVE",
  DELETED = "DELETED",
  REJECTED = "REJECTED",
  ACCEPTED = "ACCEPTED",
  PENDING = "PENDING",
  APPROVED = "APPROVED",
  CANCEL = "CANCEL",
  TERMINATE = "TERMINATE",
  PROMOTED = "PROMOTED",
  HOLD = "HOLD",
}

// define enum for attendance method
export enum AttendanceMethodEnum {
  MOBILE = "MOBILE",
  WEB = "WEB",
  BIOMETRIC = "BIOMETRIC",
  QR = "QR",
}

export enum AttendanceMethodNames {
  MOBILE = "Mobile",
  WEB = "Web",
  BIOMETRIC = "Biometric",
  QR = "QR",
}